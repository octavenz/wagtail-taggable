[flake8]
enable-extensions = G
format = pylint

ignore =
    # line too long
    E501,

exclude =
    # No need to traverse our git directory
    .git,
    # There's no value in checking cache directories
    __pycache__,
    # Would just give us 'imported but unused' errors
    __init__.py,
